name: Deploy Frontend
on:
  push:
    branches: [ "main" ]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: "eu-north-1"

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS CLI
        run: |
          aws configure set aws_access_key_id $AWS_ACCESS_KEY_ID
          aws configure set aws_secret_access_key $AWS_SECRET_ACCESS_KEY
          aws configure set region $AWS_DEFAULT_REGION

      - name: Install and Build
        run: |
          cd frontend
          npm ci
          npm run build

      - name: Get Terraform Outputs
        id: terraform-outputs
        run: |
          echo "s3_bucket=$(aws ssm get-parameter --name '/my-app/s3-bucket' --query 'Parameter.Value' --output text)" >> $GITHUB_OUTPUT
          echo "cf_dist_id=$(aws ssm get-parameter --name '/my-app/cf-dist-id' --query 'Parameter.Value' --output text)" >> $GITHUB_OUTPUT

      - name: Deploy to S3
        run: |
          # Upload static assets with long cache
          aws s3 sync ./frontend/dist s3://${{ steps.terraform-outputs.outputs.s3_bucket }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*.html" \
            --exclude "*.json"
          
          # Upload HTML files with no cache
          aws s3 sync ./frontend/dist s3://${{ steps.terraform-outputs.outputs.s3_bucket }} \
            --delete \
            --cache-control "public, max-age=0, must-revalidate" \
            --exclude "*" \
            --include "*.html" \
            --include "*.json"

      - name: Invalidate CloudFront Cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ steps.terraform-outputs.outputs.cf_dist_id }} \
            --paths "/*"
